<div class="page-header">
	<h1>Profile</h1>
</div>
<div class="row">
	<div class="col-md-4">
		<form {{action 'changePassword' on="submit"}} class="form-signin">
			{{input type="password" value=password placeholder="password..." class="form-control"}}
			{{input type="password" value=passwordRepeat placeholder="password repeat..." class="form-control bottom"}}
			{{input type="submit" value="Change password" class="btn btn-lg btn-primary btn-block"}}
		</form>
	</div>
</div>
